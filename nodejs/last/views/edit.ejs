<html>
	<head>
		<title></title>
	</head>
	<body>
		
		<h1>修改学生</h1>
		
		<form action="/doedit/<%=student.sid%>" method="get">
			
			<p>
				学号：<input type="text" name="sid" disabled="disabled" value="<%=student.sid%>" />
			</p>

			<p>
				姓名：<input type="text " name="name" value="<%=student.name%>" />
			</p>
			
			<p>
				年龄： <input type="text" name="age" value="<%=student.age%>" />
			</p>

			<p>
				性别：
				<input type="radio" name="sex" value="男" <% if( student.sex == '男' ) {%> checked <%}%> /> 男
				<input type="radio" name="sex" value="女" <% if( student.sex == '女' ) {%> checked <%}%> /> 女
			</p>
			
			<p>
				选修课程：
				
				<% for( var i=0; i<quanbuKecheng.length; ++i ){
					
					if ( student.Kechengs.indexOf(quanbuKecheng[i].kid) != -1 ) {	 	
						var sss = 'checked';
					} else {
						var sss = '';						
					}
				%>
					
					<input type="checkbox" name="Kechengs" value="<%=quanbuKecheng[i].kid %>" <%= sss %> /><%=quanbuKecheng[i].name %>
				
				<% } %>
				
			</p>
			
			<p>
				<input type="submit" value="提交" />
			</p>

		</form>
		
	</body>
</html>